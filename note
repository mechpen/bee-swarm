* cv2

cv2.imwrite("screen_max.ppm", cv2.cvtColor(max_img, cv2.COLOR_GRAY2RGB))
img1 = cv2.cvtColor(img1, cv2.COLOR_BGR2GRAY)

cv2.imshow("img", max_img)
cv2.waitKey(0)
cv2.destroyAllWindows()

* matplotlib

import matplotlib.pyplot as plt

plt.plot(scan)
plt.plot(peaks, scan[peaks], "x")
plt.show()
